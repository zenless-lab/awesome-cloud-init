#cloud-config
# Minimal configuration to install a C++ development environment.
# Optimizes package definitions by only branching where names differ between distributions.

# Install core packages and development tools
packages:
  - apt:
    - build-essential
    - pkg-config
  - yum:
    - gcc-c++
    - make
    - pkgconf-pkg-config
  - clang
  - lld
  - lldb
  - cmake
  - meson
  - ninja-build
  - gdb
  - valgrind
  - ccache
  - git
  - curl
